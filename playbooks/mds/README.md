# Cisco MDS Playbooks

This directory will contain read-only MDS inventory first, followed by
review-gated VSAN, device-alias, zone, and zoneset automation.

Zoneset activation must remain an explicit, approved operation.
